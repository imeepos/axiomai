async load(where: sdk.LocPresenterController.LoadQuery) {
    return await this.prisma.presenter.findMany({
      where,
      include: { _count: { select: { projects: true, products: true } }, liveRooms: true },
    });
  }